#include<bits/stdc++.h>

using namespace std;


int main(){
    int T;
    cin>>T;

    for (int c = 0; c < T; c++)
    {
        int N,K;
        cin>>N;
        vector<int> A(N);
        vector<int> sums(N);
        for (int i = 0; i < N; i++)
        {
            cin>>A[i];
        }
        for (int i = 1; i < N+1; i++)
        {
            for (int j = 0; j < i; j++)
            {
                
            }
            
        }
        
        cout<<"Case #"<<c+1<<": "<<count<<"\n";
        
        
    }
    





    return 0;
}